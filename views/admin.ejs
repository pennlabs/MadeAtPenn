<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
 <link href='http://fonts.googleapis.com/css?family=Lato:300,400,700,900italic' rel='stylesheet' type='text/css'>
        <!-- Bootstrap core CSS -->
     <link href="/style.css" rel="stylesheet" type="text/css">

    <link href="/bootstrap.css" rel="stylesheet">

    <meta name="description" content="">
    <meta name="author" content="">
    <title>Admin Page</title>
  
  </head>

  <body>

     <% include banner %>

     <a href="/logout"> Logout </a>

    <h1> Unapproved Projects </h1>
    <% projects.forEach(function(project, index){ %>
      
       <!-- fix this to make it approved == 1 -->
        <% if (project.approved == 0 && (project.project_or_startup == "project" || project.project_or_startup == '')) { %>
              <div class="project" id= <%= project._id %> >
                <h2> <a href=<%= project.demo_link %>> <%= project.app_name %> </a></h2>
                <div id="app_image">
               <img width='300px' height='200px' src= <%= "https://s3-us-west-2.amazonaws.com/penn-open-source/images/" + project._id + ".jpg" %> />
                </div>
                <p> <%= project.description %> </p>
                <p> built by: <%= project.builders %>, at <%= project.location %> </p>
                    <% if (project.github_permis == 'yes') { %>
                        <p> <a href= <%= project.github %> > Github </a> </p>
                    <% } %>

                <p> Tags: <%  %>
                  <% var array = project.tags.split(","); %>

                  <% for(var i=0; i<array.length; i++) { %>
                  <a href =  <%= '/tag/' + array[i].trim() %> > <%= array[i] %></a>  
                  <% } %>
                </p>
             <a href = /edit/<%= project._id %> >Edit </a>
              </div>
        <% } %> 
    <% }) %>


 <h1> Unapproved Startups </h1>
    <% projects.forEach(function(project, index){ %>
      
       <!-- fix this to make it approved == 1 -->
        <% if ((project.approved == 0) && (project.project_or_startup == "startup" || project.project_or_startup == '')) { %>
              <div class="project" id= <%= project._id %> >
                <h2> <a href=<%= project.demo_link %>> <%= project.app_name %> </a></h2>
                <div id="app_image">
               <img width='300px' height='200px' src= <%= "https://s3-us-west-2.amazonaws.com/penn-open-source/images/" + project._id + ".jpg" %> />
                </div>
                <p> <%= project.description %> </p>
                <p> built by: <%= project.builders %>, at <%= project.location %> </p>
                    <% if (project.github_permis == 'yes') { %>
                        <p> <a href= <%= project.github %> > Github </a> </p>
                    <% } %>

                <p> Tags: <%  %>
                  <% var array = project.tags.split(","); %>

                  <% for(var i=0; i<array.length; i++) { %>
                  <a href =  <%= '/tag/' + array[i].trim() %> > <%= array[i] %></a>  
                  <% } %>
                </p>
             <a href = /edit/<%= project._id %> >Edit </a>
              </div>
        <% } %> 
    <% }) %>




    <h1> Approved Projects </h1>
 <% projects.forEach(function(project, index){ %>
      
       <!-- fix this to make it approved == 1 -->
        <% if (project.approved == 1 && project.project_or_startup == "project") { %>
              <div class="project" id= <%= project._id %> >
                <h2> <a href=<%= project.demo_link %>> <%= project.app_name %> </a></h2>
                <div id="app_image">
               <img width='300px' height='200px' src= <%= "https://s3-us-west-2.amazonaws.com/penn-open-source/images/" + project._id + ".jpg" %> />
                </div>
                <p> <%= project.description %> </p>
                <p> built by: <%= project.builders %>, at <%= project.location %> </p>
                    <% if (project.github_permis == 'yes') { %>
                        <p> <a href= <%= project.github %> > Github </a> </p>
                    <% } %>

                <p> Tags: <%  %>
                  <% var array = project.tags.split(","); %>

                  <% for(var i=0; i<array.length; i++) { %>
                  <a href =  <%= '/tag/' + array[i].trim() %> > <%= array[i] %></a>  
                  <% } %>
                </p>
             <a href = /edit/<%= project._id %> >Edit </a>
              </div>
        <% } %> 
    <% }) %>


    <h1> Approved Startups </h1>
 <% projects.forEach(function(project, index){ %>
      
       <!-- fix this to make it approved == 1 -->
        <% if (project.approved == 1 && project.project_or_startup == "startup") { %>
              <div class="project" id= <%= project._id %> >
                <h2> <a href=<%= project.demo_link %>> <%= project.app_name %> </a></h2>
                <div id="app_image">
               <img width='300px' height='200px' src= <%= "https://s3-us-west-2.amazonaws.com/penn-open-source/images/" + project._id + ".jpg" %> />
                </div>
                <p> <%= project.description %> </p>
                <p> built by: <%= project.builders %>, at <%= project.location %> </p>
                    <% if (project.github_permis == 'yes') { %>
                        <p> <a href= <%= project.github %> > Github </a> </p>
                    <% } %>

                <p> Tags: <%  %>
                  <% var array = project.tags.split(","); %>

                  <% for(var i=0; i<array.length; i++) { %>
                  <a href =  <%= '/tag/' + array[i].trim() %> > <%= array[i] %></a>  
                  <% } %>
                </p>
             <a href = /edit/<%= project._id %> >Edit </a>
              </div>
        <% } %> 
    <% }) %>


    <% include footer %>



  </body>


  </html>